import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottquach.pushupcounter',
  appName: 'Push-Up Counter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
